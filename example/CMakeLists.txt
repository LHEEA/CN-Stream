add_subdirectory(C++)
if(UNIX)
    message(STATUS "Activating fortran example")
    add_subdirectory(fortran)
else()
    message(STATUS "Deactivating fortran example because of shared library loading implemented only for so file")
endif()